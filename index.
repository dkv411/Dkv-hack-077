<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>UC Marketplace - Demo</title>
  <style>
    body{font-family:Arial, Helvetica, sans-serif;background:#0b1020;color:#e6f7ff;display:flex;align-items:center;justify-content:center;height:100vh;margin:0}
    .card{background:#071029;border-radius:12px;padding:22px;width:320px;box-shadow:0 10px 30px rgba(0,0,0,0.6);border:1px solid rgba(0,255,153,0.06)}
    h2{margin:0 0 6px;font-size:20px;color:#00ff99}
    p{margin:6px 0;color:#9bdac9}
    .price{font-size:24px;font-weight:700;margin:12px 0;color:#fff}
    .btn{display:inline-block;background:#00ff99;color:#001;border:none;padding:10px 14px;border-radius:8px;cursor:pointer;text-decoration:none;font-weight:700}
    .small{font-size:12px;color:#b8f2d8;margin-top:8px}
  </style>
</head>
<body>
  <div class="card">
    <h2>500 UC Pack</h2>
    <p>Game: ExampleGame • Seller: <strong>ProSeller</strong></p>
    <div class="price">₹149.00</div>
    <a class="btn" href="#" onclick="buyNow(event)">Buy Now</a>
    <div class="small">Immediate delivery: code sent via email after payment</div>
  </div>

  <script>
    function buyNow(e){
      e.preventDefault();
      // demo: show a checkout prompt — replace with actual checkout integration (Razorpay/Stripe)
      const ok = confirm("Proceed to payment of ₹149?");
      if(!ok) return;
      // In real app: redirect to backend checkout endpoint which creates payment order
      alert("Demo: payment flow would start now. Backend integration required.");
    }
  </script>
</body>
</html>
